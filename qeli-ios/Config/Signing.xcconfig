// Override these values locally or in CI. Do not commit provisioning profiles.
DEVELOPMENT_TEAM =
QELI_APP_BUNDLE_ID = ru.autocash.qeli
QELI_TUNNEL_BUNDLE_ID = $(QELI_APP_BUNDLE_ID).PacketTunnel
QELI_WIDGET_BUNDLE_ID = $(QELI_APP_BUNDLE_ID).Widgets
QELI_APP_GROUP = group.$(QELI_APP_BUNDLE_ID)
QELI_KEYCHAIN_GROUP = $(AppIdentifierPrefix)$(QELI_APP_BUNDLE_ID).shared
